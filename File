MModAPI.require('player');
ModAPI.addEventListener("key", function(ev) {
    if (ev.key == 34) {
        if ('/gamemode creative AceTest1'.charAt(0) !== "/") {
            ModAPI.player.sendChatMessage({
                message: '/gamemode creative AceTest1'
            })
        }
    }
});
